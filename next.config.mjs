/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.cervelo.com', 'assets.specialized.com', 'enve.com', 'giant-bicycles.com', 'cervelo.com', 'specialized.com'],
    }
};

export default nextConfig;
