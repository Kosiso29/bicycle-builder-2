/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.cervelo.com', 'assets.specialized.com', 'www.enve.com', 'enve.com', 'images2.giant-bicycles.com', 'giant-bicycles.com', 'cervelo.com', 'specialized.com'],
    }
};

export default nextConfig;
